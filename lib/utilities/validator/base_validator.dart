abstract class BaseValidator {

  RegExp? regExp;
  //
  // bool isValid({required bool validator}) {
  //   return validator;
  // }
  //
  // void getRegExp({required String pattern}){
  //   regExp = RegExp(pattern);
  // }
}
