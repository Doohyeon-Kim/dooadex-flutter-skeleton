name: dooadex_flutter_skeleton
description: A Flutter skeleton project made by Doohyeon Kim.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dooadex_logger:
  dooadex_constants:
  dooadex_palette:
  dooadex_error_handler:
  dooadex_http:
  provider: ^6.0.3
  go_router: ^4.0.0
  google_fonts: ^3.0.1
  auto_size_text: ^3.0.0
  geolocator: ^8.2.0
  connectivity_plus: ^2.2.1
  shared_preferences: ^2.0.13
  flutter_secure_storage: ^5.0.2
  kakao_flutter_sdk: ^1.1.1
  getwidget: ^3.0.1
  flutter_local_notifications: ^9.4.0
  intl: ^0.17.0


  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  logger: ^1.1.0
  json_serializable: ^6.1.5
  json_annotation: ^4.4.0
  build_runner: ^2.1.8
  mockito: ^5.1.0
  test: ^1.19.5

  flutter_lints: ^2.0.1


flutter:

  uses-material-design: true
